---
title: Introduction
description: An MCP server, AI guidelines, and agent skills that teach your coding agent your NestJS app.
---

# nest-boost

**Laravel Boost, for NestJS.**

An MCP server, AI guidelines, and agent skills that teach your coding agent how *your*
NestJS application is actually wired — its real modules, routes, and dependency-injection
graph — and how to write idiomatic Nest code.

nest-boost is a **Node.js** CLI (developed with [Bun](https://bun.sh)) that speaks the
[Model Context Protocol](https://modelcontextprotocol.io). The package is published as
`@mgvdev/nest-boost`.

## Why

AI coding agents are great at NestJS in the abstract, but they don't know *your* app. They
guess module boundaries, invent routes that already exist, and follow whatever architecture
they saw last. nest-boost closes that gap: it gives the agent live, grounded knowledge of
your codebase over MCP, plus curated best-practice guidance tuned to the packages and
conventions you actually use.

It's the NestJS counterpart to [Laravel Boost](https://github.com/laravel/boost).

## What you get

| Piece | What it does |
| --- | --- |
| **MCP server** | Boots your app and exposes tools to inspect its modules, routes, and DI graph |
| **AI guidelines** | Best-practice conventions, composed from the packages you use, loaded up front |
| **Agent skills** | On-demand, task-specific playbooks (testing, architecture, auth, per-library) |
| **`skill-builder`** | A skill that writes *new* skills for libraries nest-boost doesn't cover yet |

One command wires all of it into your agent of choice.

## Requirements

- [Node.js](https://nodejs.org) `>= 18.18` (Bun also works)
- A NestJS project (`>= 9`) whose `tsconfig.json` has `emitDecoratorMetadata` (the Nest default)
- SQLite database tools also need Node `>= 22` (built-in `node:sqlite`) or the `better-sqlite3` package

## Supported agents

| Agent | MCP | Guidelines | Skills |
| --- | :---: | :---: | :---: |
| Claude Code | `.mcp.json` | `CLAUDE.md` | `.claude/skills/` |
| Cursor | `.cursor/mcp.json` | `.cursor/rules/` | — |
| Codex | *(CLI)* | `AGENTS.md` | — |
| Gemini CLI | `.gemini/settings.json` | `AGENTS.md` | — |
| Generic | `.mcp.json` | `AGENTS.md` | — |

## Links

- **GitHub** — [github.com/mgvdev/nest-boost](https://github.com/mgvdev/nest-boost)
- **npm** — [@mgvdev/nest-boost](https://www.npmjs.com/package/@mgvdev/nest-boost)

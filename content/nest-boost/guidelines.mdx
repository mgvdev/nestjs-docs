---
title: Guidelines
description: Best-practice conventions composed from the packages your project uses.
---

# Guidelines

Guidelines are Markdown loaded up front. nest-boost composes them from what your project
uses — core Nest conventions, a version note, your chosen architecture and auth strategy,
and one section per detected package (TypeORM, Prisma, GraphQL, Mongoose, config,
validation, Swagger, queues, testing…).

They're written into the agent's guideline file (`CLAUDE.md`, `AGENTS.md`, a Cursor rule, …)
between managed markers, so **your own content around them is preserved** and the block is
regenerated by `nest-boost update`.

## What gets composed

| Category | Guidelines |
| --- | --- |
| **Core** | Core Nest conventions, NestJS 11 version notes |
| **Architecture** | Standard layered · CQRS · Hexagonal |
| **Auth** | Passport · Better Auth |
| **ORMs / ODMs** | TypeORM · Prisma · Mongoose |
| **API** | GraphQL · Swagger |
| **Cross-cutting** | Config · Validation · Queues · Testing · Monorepo |
| **Test layout** | Colocated · Colocated subfolder · Central |

Only the sections relevant to your detected dependencies and chosen options are included, so
the agent's context stays focused on conventions you actually use.
